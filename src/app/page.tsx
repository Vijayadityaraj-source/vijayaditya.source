import {turrent_road} from './fonts'

export default function Home(){
  return (
    <div className="main">
        <h1 className={`${turrent_road.className} home`}>vijayaditya.source</h1>
    </div>
  )
}